# 报销截图转Excel - FC 3.0 项目日志

## 项目概述

将本地 Python 报销截图识别工具改为阿里云函数计算（FC 3.0）Serverless 网页应用。

## 最终成果

- **网页前端**: https://seiner69.github.io/baoxiao-tool/
- **FC函数**: 处理图片OCR + DeepSeek信息提取 + Excel生成
- **OSS**: 存储生成的Excel文件

---

## 时间线与关键节点

### 2026-04-03 晚

| 时间 | 事件 |
|------|------|
| 初始需求 | 将本地脚本改为FC 3.0版本 |
| 第一次尝试 | 使用企业微信对接（被拒绝）|
| 第二次尝试 | 改为纯网页版，HTTP请求响应 |

### 遇到的问题

#### 1. cryptography版本冲突
- **错误**: `Module initialization error: 'module 'lib' has no attribute 'X509_V_FLAG_NOTIFY_POLICY'`
- **原因**: SDK与FC环境Python版本不兼容
- **解决**: 完全移除第三方SDK，使用原生urllib

#### 2. 触发器响应模式
- **问题**: 默认域名强制下载HTML
- **原因**: 没有勾选"响应HTML(Integrate Response)"
- **解决**: 改为纯API返回JSON，前端独立托管

#### 3. 前端托管 - GitHub Pages
- **问题**: file://协议不能跨域
- **解决**: 部署到GitHub Pages

#### 4. 公网无法访问 - 关键坑！
- **现象**: `ERR_CONNECTION_TIMED_OUT`
- **原因**: URL包含`vpc`后缀，只能内网访问
- **发现**: 还有另一个公网URL `*.fcapp.run`（无vpc）
- **解决**: 使用公网URL

#### 5. method解析错误 - 最反复的问题
- **现象**: 日志显示`method=GET`，实际应是`POST`
- **原因**: event是multipart bytes数据，不是JSON
- **尝试修复**: 加正则提取 → 加各种条件判断
- **最终解决**: 检测到multipart直接识别为POST

---

## 做得好的地方

1. ✅ 使用免费OCR.space API，避免阿里云SDK问题
2. ✅ 返回JSON而非HTML，绕过FC响应模式问题
3. ✅ 前端独立托管，灵活部署
4. ✅ 反复调试，日志清晰

---

## 做得不好的地方

1. ❌ 最初执着于阿里云SDK，应该直接用免费API
2. ❌ 在method解析上花费太多时间（3次重写）
3. ❌ 没有第一时间用公网URL（`*cn-hangzhou.fcapp.run`）
4. ❌ 代码写得不够干净，有残留代码

---

## 反复出错的核心原因

```
event参数类型不固定:
- FC测试界面: bytes(JSON字符串)
- 真实HTTP: dict 或 bytes(multipart)
```

代码需要同时处理三种情况：
1. `isinstance(event, bytes)` - 测试JSON或multipart
2. `isinstance(event, dict)` - 真实HTTP请求
3. 嵌套在bytes里的multipart数据

---

## 后续优化

1. **代码清理**: 删除调试日志，优化multipart解析
2. **OCR优化**: 免费API有每天500次限制，考虑阿里云付费方案
3. **DeepSeek**: 确保环境变量正确配置
4. **自定义域名**: 如果公网URL不稳定，配置自定义域名

---

## 项目文件

```
报销截图FC/
├── index.py          # FC函数代码
├── web/
│   └── index.html   # 前端页面
├── requirements.txt # 依赖
├── s.yml           # FC配置
└── README.md       # 说明文档
```

---

## 总结

| 项目 | 状态 |
|------|------|
| FC函数部署 | ✅ 完成 |
| 前端页面 | ✅ 完成 |
| GitHub Pages | ✅ 完成 |
| 手机访问 | ⚠️ 公网URL可用 |
| OCR识别 | ⚠️ 待测试 |
| DeepSeek提取 | ⚠️ 待测试 |